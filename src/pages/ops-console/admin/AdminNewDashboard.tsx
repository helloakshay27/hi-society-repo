import React from "react";

/**
 * Blank, standalone admin-console page/route — not tied to any existing
 * feature. Build the new UI here.
 */
export const AdminNewDashboard: React.FC = () => {
  return (
    <div className="p-2 sm:p-4 lg:p-6 max-w-full overflow-x-hidden">
      <h1 className="text-xl sm:text-2xl font-bold text-[#1A1A1A]">New Dashboard</h1>
    </div>
  );
};

export default AdminNewDashboard;
