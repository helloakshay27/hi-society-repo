import React from 'react';
import { PostPossessionDashboard } from './post-possession-dashboard';

/**
 * Post Possession admin dashboard — replica of the Veda Gardens reference UI
 * with all tabs, drill panels, charts, filters, and AI copilot.
 */
export const AdminNewDashboard: React.FC = () => {
  return <PostPossessionDashboard />;
};

export default AdminNewDashboard;
